const list=document.getElementById('journalist-list');const form=document.getElementById('journalist-search');const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function photo(j){const initials=j.name.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase();const url=/^https?:\/\//i.test(j.image_url||'')?` style="background-image:url('${esc(j.image_url)}')"`:'';return `<span class="directory-avatar${url?' has-photo':''}"${url}>${url?'':esc(initials)}</span>`}async function load(q=''){list.innerHTML='<p>Loading journalists…</p>';try{const res=await fetch(`/api/journalists?q=${encodeURIComponent(q)}`);const data=await res.json();list.innerHTML=data.items.length?data.items.map(j=>`<a class="directory-card" href="${esc(j.profile_url)}">${photo(j)}${j.verified?'<span class="verified-label">✓ VERIFIED JOURNALIST</span>':''}<h2>${esc(j.name)}</h2><p>${esc(j.beat||'Journalist')}</p><p>${esc(j.location||'Uganda')}</p><small>${Number(j.published_story_count||0)} published ${Number(j.published_story_count)===1?'story':'stories'}</small></a>`).join(''):'<p>No matching journalists found.</p>'}catch(_){list.innerHTML='<p>The journalist directory is temporarily unavailable.</p>'}}form.addEventListener('submit',e=>{e.preventDefault();load(new FormData(form).get('q').trim())});load();
